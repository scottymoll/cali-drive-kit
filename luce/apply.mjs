// apply.mjs — disabled in review-only baseline.
// Keeping this stub avoids import errors if other files import applyCursorPrompt.
export async function applyCursorPrompt() {
  console.log('applyCursorPrompt disabled (review-only baseline).');
}