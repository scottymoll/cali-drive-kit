# Delta Change Request Template
1) Selector: {SEL} — Change {PROP} from {FROM} to {TO}. Why: {WHY}.
2) Selector: {SEL2} — Insert {HTML2} after {TARGET2}. Why: {WHY2}.
Constraints: Keep layout stable; CLS <0.1.
